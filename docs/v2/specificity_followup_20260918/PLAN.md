# Specificity follow-up specification

Date: 2026-09-18. Parent: 1c7983e. Previous findings are known; these are requested sensitivity analyses, not independent confirmation. Commit this plan before running either new human analysis or any mouse disruption/knockdown calculation. Preserve every outcome and the original matched-perturbation result.

## Availability and immediate stop

The current workspace lacked the later commits and reconstruction cache. Restore the exact 1c7983e history from the user's already-pushed remote branch. The saved human matrices and R 4.3.3 runtime are available. The selected-vector mouse cache is missing. Moreover, that cache had 245 selected vectors, not a whole-transcriptome matrix. Restricting a global metric to those endpoint/target-selected genes would be biased. Do not substitute such a metric. Mouse analysis is BLOCKED_INPUT pending broad expression acquisition. No new mouse response or disruption calculation will run under this blocked state. Public-source metadata inspection identifies a 35.06 GB compressed preprocessed expression archive and per-batch h5ad files; neither has been downloaded for this follow-up. No PowerShell Git recovery is needed because the remote is intact.

## 1. Mouse global-disruption design, not yet executed

Retain the original selected comparator union and Naa20/Pomp/Psmc5 focal targets; retain Psmb4 for descriptive realized-knockdown reporting without rescuing its unevaluable primary matched test. No new comparators. Use the same original per-mouse neuronal-group contrasts, retention and control-count criteria.

For each gene perturbation and mouse, define global disruption as the root mean square of its within-mouse, neuronal-group-adjusted expression differences from non-targeting centroids, over all genes in the original browser expression index except the frozen 85 synaptic genes and all target/comparator genes. Aggregate the stratum differences with the original target-cell weights before calculating RMS. Use the existing natural log(1+10000*gene UMI/total RNA UMI) expression scale. Do not select background genes using perturbation outcomes. Zeros remain valid; do not take only significant genes. Save the exact background list and source reconciliation before fitting. This is a transcriptomic-distance proxy, not a direct viability measurement.

Realized target-gene knockdown is its matched target-minus-non-targeting log1p contrast; also save the control centroid. Call knockdown measurable only with control mean >=0.1 log1p units and >=10% control-cell detection in the relevant weighted strata. Positive contrasts remain positive, not truncated to knockdown. No model-driven imputation for unmeasurable targets.

Primary descriptive regression uses comparator-only mouse observations: synaptic difference ~ global RMS + realized target-expression difference + log(retained target cells) + mouse fixed effects, with a random intercept for perturbed-gene identity. Fit by REML. A gene identity fixed effect cannot supply an identified prediction for a focal gene excluded from fitting, so gene identity enters as a random intercept; held-out focal predictions use its population mean of zero. Include no interactions, polynomial terms or outcome-selected covariates. Require >=20 comparator genes with measurable knockdown, >=3 eligible mice/gene, full-rank fixed design, converged model and positive residual variance. If fitting or support fails, report NOT_EVALUABLE; no simpler favorable replacement model. Report predictor overlap, estimated coefficients and each focal's residual from the comparator fit. Explicitly flag focal observations outside the comparator range of either continuous biological predictor; report these as extrapolations. Average residuals first within mouse, then within litter. Descriptive uncertainty, if evaluable, uses 1,000 whole-litter resamples with refitting (seed 20260918); require >=900 successful fits. No hypothesis of exclusion of the disruption confound and no new success/failure label.

Also plot equal-mouse mean synaptic response against the independent dependency percentile for every comparator gene and focal target. Report comparator-only Pearson and Spearman associations and sample size as descriptive, without treating mouse/cell repetitions as gene replicates. A weak association cannot exclude a moderate trend with these finite panels. This plot can be created from committed aggregate tables, but will be clearly separated from the blocked disruption regression.

## 2. Human permutation-consistent intervals

Use exactly the four saved relative-culture contrasts, five control and five NAA25-knockdown cultures. The statistic is the unstudentized difference in culture means, matching the existing exact two-sided permutation p values over all 252 assignments. For each candidate constant additive treatment shift tau, subtract tau from the observed treated cultures and recompute the exact two-sided permutation test. The 95% confidence set is all tau with p_tau >0.05. Calculate its exact boundaries from the finite linear-statistic breakpoints; enumerate interval interiors and boundary points and verify that the returned set is connected, otherwise report the full union. Include ties using numerical tolerance 1e-12. Do not replace an inconvenient interval with Welch or bootstrap results.

This interval inverts an exchangeability test under a constant additive-shift model. It is not an assumption-free heterogeneous-effect interval or a simultaneous four-contrast interval. Display the original Welch intervals as a labeled comparison, preserve exact p values and BH4, and use inverted intervals in the new figure.

For this equal-size, two-sided test, every assignment has a complementary assignment with the same absolute statistic. Thus the minimum attainable two-sided p is 2/252 = 0.0079365; 1/252 applies to a directional extreme, not this two-sided test. The two-sided 95% inversion is discrete/conservative. More Monte Carlo permutations cannot add assignments.

## 3. CAMERA correlation sensitivity

Use the same complete-case primary protein matrix (3,409 proteins), two-group design and all Reactome sets with 10-500 measured proteins (862 sets). R 4.3.3, limma 3.58.1, statmod 1.5.0. Run CAMERA at common fixed correlations 0, .005, .01, .0125, .015, .02, .03, .05, .075, .10, .15, .20 and .30. Also run the original estimated-correlation procedure. Save all pathway rows at every setting, with BH FDR calculated over the same full eligible family separately at each setting. Plot chemical-synapse and neurotransmitter-release FDR versus the fixed correlation, with 0.05 reference and a separate annotation for estimated correlation/p/FDR. Do not imply that estimating a different correlation for every pathway is the same test as a common fixed-correlation grid point. Inspect and report any other CAMERA inference differences from the installed function. Report each tested grid value's status; if there is a transition, locate it only between adjacent tested grid values without interpolation claims. Original null and positive results remain unchanged.
