# Item 1 runner recovery, 2026-09-19

Parent: 0d9327216487bed9e73dd76e762ba4250e0f539b. This restores execution of the statistical specification committed at ee992a0 in docs/v2/specificity_followup_20260918/PLAN.md. It does not add a hypothesis or change comparators, endpoints, covariates, or success rules.

The previously delivered 7e2c86b bundle, runner and first-batch reduction disappeared from the transient workspace before the user downloaded them. Those commits are absent from the available object database and remote. Reconstructed software receives new commits and checksums. Prior execution claims are historical and are not validation of this new implementation. All 14 original raw batches must be processed again. The immutable source revision and publisher LFS hashes are in batch_sources.json.

Keep the original 51-comparator union and four focal genes. Primary predictions concern Naa20, Pomp and Psmc5; Psmb4 remains descriptive. Read every gene in the original browser expression index, excluding the non-gene `_range` metadata key. Exclude the frozen 85 synaptic genes and all target/comparator genes from the global RMS background, without outcome-based filtering. Expected biological universe 19,070; expected background 18,931. Stop on mismatch.

Use raw integer RNA counts in the original H5AD matrices, normalize by author num_rna_umi, and compare the resulting natural-log expression scale with the original browser values. Require exact cell identity, author QC, one guide, neuronal eligibility, original browser inclusion, >=20 control cells per mouse/group, >=20 retained target cells and >=80% target retention. Aggregate the same stratum differences with original target-cell weights before computing RMS. Reconstruct every eligible original synaptic contrast within 1e-6 before permitting model fitting.

Save source checksums, background membership, selected cell IDs, aggregation weights and per-batch reduced matrices. Reuse a checkpoint only if its source, file hash and full preparation fingerprint match. Release only runner-owned raw cache files after successful verified reduction. Do not delete user project inputs.

Fit the frozen comparator-only REML model with gene random intercept and mouse fixed effects. Validate the custom profiled scalar variance-ratio REML against statsmodels on synthetic data and on the observed design before resampling. A zero gene-variance estimate is an allowed boundary of the same model, not a fallback. Reject non-finite or rank-deficient designs, nonpositive residual variance, optimizer failures, and a solution at the artificial upper search limit. No simpler replacement fit.

Realized target-expression contrast is measurable only for weighted control mean >=0.1 and weighted detection >=0.10. Qualify comparator genes with >=3 measurable eligible mice, requiring >=20 genes. Freeze that qualified set for bootstrap resampling; require >=20 represented genes in each fit. Repeated whole-litter copies get distinct mouse and litter labels while retaining gene identities. Use 1,000 draws and seed 20260918, requiring >=900 successful fits and >=900 available summaries per focal gene for descriptive percentile intervals. Retain every failed draw. Focal population predictions use gene intercept zero; mice absent from comparator training remain unevaluable. Report biological predictor range extrapolations and in-range subsets alongside all available residuals. No claim that a confidence interval excludes the confound.

Recover litter identities from the author's unchanged media-3.xlsx content, expected SHA256 7c534d8fdce8f47de4df84982ed720f5cec1b1636f89a230be83e27484e1fc49; preserve historical ZIP-container discrepancy and verify identities against previously tracked mappings. Fail on mismatches or missing mouse mappings. No inferred litter identities.

Source acquisition may run to inspect storage/schema and test scale. No full disruption calculation or observed model will run before this plan is committed. If cloud acquisition is blocked, package the tested runner for local execution and state explicitly that observed results are pending. Synthetic checks do not count as biological results.
